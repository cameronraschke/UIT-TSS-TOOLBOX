server {
        listen UIT_LAN_IP_ADDRESS:81 default_server;

        root /srv/uit-toolbox/;

        index index.html index.htm index.nginx-debian.html;

        server_name _;

        location / {
                try_files $uri $uri/ =404;
        }
}
