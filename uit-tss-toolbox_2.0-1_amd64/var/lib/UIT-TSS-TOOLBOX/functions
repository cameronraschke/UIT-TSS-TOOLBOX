#!/usr/bin/php
<?php

function filter ($string) {
    if ($string == "" || $string == " " || $string == "NULL" || empty($string) || is_null($string) || count($string) == 0) {
        return 1;
    } else {
        return 0;
    }
}

function clearArray() {
    unset($GLOBALS["arr"]);
}

?>